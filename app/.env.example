VITE_BASE_URL=http://localhost:3000

DATABASE_URL="postgresql://user:password@localhost:5432/tanstarter"
# You can also use Docker Compose to set up a local PostgreSQL database:
# docker-compose up -d

# pnpm run auth:secret
BETTER_AUTH_SECRET=

# OAuth2 providers, optional, update as needed
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# NOTE:
# In your OAuth2 apps, set callback/redirect URIs to`http://localhost:3000/api/auth/callback/<provider>`
# e.g. http://localhost:3000/api/auth/callback/github

# ==== Gmail (permissions & defaults) ====
# We will request additional scopes via link flow in-app.
# These are the scopes we will request:
#  - https://www.googleapis.com/auth/gmail.readonly
#  - https://www.googleapis.com/auth/gmail.modify
#  - https://www.googleapis.com/auth/gmail.send
GOOGLE_SCOPES="https://www.googleapis.com/auth/gmail.readonly https://www.googleapis.com/auth/gmail.modify https://www.googleapis.com/auth/gmail.send"
# Default Gmail search query used when loading emails
GMAIL_LABEL_QUERY=label:customer-inquiries newer_than:7d

# ==== Shopify (App OAuth) ====
# Your Shopify custom app credentials
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
# Broad scopes for retail admin actions (adjust as needed)
SHOPIFY_SCOPES=read_orders,write_orders,read_customers,write_customers,read_fulfillments,write_fulfillments,read_assigned_fulfillment_orders,write_assigned_fulfillment_orders,read_draft_orders,write_draft_orders,read_products,write_products,read_inventory,write_inventory,read_shipping,write_shipping,read_locations
# OAuth callback for the app (route will be implemented in TanStack API routes)
SHOPIFY_REDIRECT_URI=http://localhost:3000/api/shopify/callback
# Default shop domain (editable in Settings UI)
SHOPIFY_SHOP=r901.myshopify.com

# Optional: Shopify REST API version used by helpers/tools (e.g., 2024-10)
SHOPIFY_API_VERSION=2024-10

# ==== Encryption Key ====
# 32-byte hex (64 chars) for AES-256-GCM used to encrypt tokens
MASTER_KEY=