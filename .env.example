# Global master key for encrypting secrets at rest (AES-256-GCM). Generate with: openssl rand -hex 32
MASTER_KEY=

# ==== Backend (server) ====
# Public URL the backend is reachable at (used for OAuth callbacks)
SERVER_PUBLIC_URL=http://localhost:4000
PORT=4000
CORS_ORIGIN=http://localhost:5173
SESSION_SECRET=change_me_dev_only

# Google OAuth (Gmail)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:4000/auth/google/callback

# Shopify OAuth
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SHOPIFY_SCOPES=read_orders,write_orders,read_customers,write_customers
SHOPIFY_REDIRECT_URI=http://localhost:4000/auth/shopify/callback

# Mastra / LLM provider
# e.g. OPENAI_API_KEY=

# Mastra persistent memory (LibSQL)
# Override to use a custom SQLite/libsql URL.
# Examples:
#  - file storage (default): file:/absolute/path/to/memory.db
#  - local relative (resolved in code): file:../memory.db
#  - Turso/libsql remote: libsql://your-db-unique-host.turso.io
# Leave empty to use the default path resolved in code.
MASTRA_MEMORY_URL=

# Recharge API (for subscriptions)
# Required for managing subscriptions (list/cancel/pause/resume) via Recharge.
# Never commit your API key. Configure per environment.
# Base defaults to https://api.rechargeapps.com; version header optional but recommended.
RECHARGE_API_KEY=
RECHARGE_API_BASE=https://api.rechargeapps.com
# Example versions: 2021-11, 2021-01, or latest supported by your account
RECHARGE_API_VERSION=

# Optional: Map aliases to real version numbers (used when auth.version is 'latest'/'newest' or 'second-newest')
# Example:
# RECHARGE_API_VERSION_NEWEST=2024-01
# RECHARGE_API_VERSION_SECOND_NEWEST=2023-10
RECHARGE_API_VERSION_NEWEST=
RECHARGE_API_VERSION_SECOND_NEWEST=

# ==== Frontend ====
# If you need to override API base for the frontend
VITE_API_BASE=http://localhost:4000
